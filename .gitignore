# Windows / editores
Thumbs.db
Desktop.ini
*.tmp
~$*

# Ferramentas locais — não publicar (só curso + docs + scripts para alunos)
.vscode/
scripts/head_audit_once.py
scripts/push-curso.bat

# Rascunho / mineração (referência V4.4 — mantenedor)
🎓 VERSÃO 4.4 EVOLUÍDA – A OBR.md
Plano equipe
Plano equipe.md

# --- Repo público = SÓ O CURSO. Interno → pgp-equipe-dev/ (local). ---
CLAUDE.md
.claude/

# --- Doc interna equipe dev ---
pgp-equipe-dev/
MANIFEST.txt

# docs/ removido do remoto (jul/2026) — material mantenedor em pgp-equipe-dev/docs/
/docs/

# --- Arquivos compactados (backups locais) ---
*.zip
*.7z

# --- Cursor (dentro de pgp-equipe-dev/.cursor/ quando existir) ---
